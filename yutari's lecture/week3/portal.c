#include <stdio.h>
#include <string.h>

typedef struct
{
    char name[10];
    int task_score;
    int mid_score;
    int final_score;
}   Unist;
/*
void printscore(Unist* persons,int len)
{
    printf("%s %c\n",)

}
*/
int main(void)
{   
    char str[3];
    for(int i =0; i<3;i++)
    {
        scanf("%s",p[i].name);

    }
    

    for(int i=1; i<4;i++)
    {
        struct Person p1 = { .name = str[i], .task_score = 30, .mid_score = ,.final_score = };
        printf("%s %c", p1.name);    
    }


    return 0;
}
